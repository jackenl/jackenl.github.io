const bubbleSort = require('../bubbleSort')
const random = require('../random')

let len = 1000
let arr = []
for (let i = 0; i < len; i++) {
  
}
const arr = [1, 4, 2, 5, 3]
console.log(bubbleSort(arr))